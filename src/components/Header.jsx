import React from 'react';
import './styles/header.scss';


const Header = ({ filter, handleRemove, removeAll}) => {
    return (
        <div className="header">
            <div className={`${
                filters.length !== 0?
            }`}
        </div>
    )
}